# === Core LangChain ecosystem ===
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0 # 로컬 LLM(Ollama) 답변 생성 프로바이더용
langchain-chroma>=0.1.0
langchain-redis>=0.1.0
langchain-text-splitters>=0.2.0 # RecursiveCharacterTextSplitter for improved chunking

# === Vector store / retriever ===
chromadb>=0.5.5
faiss-cpu>=1.8.0.post1        # (optional, 다른 벡터 DB와 병행 가능)
redis>=5.0.0

# === Embeddings (HuggingFace + KURE-v1) ===
sentence-transformers==5.4.1
torch==2.11.0
transformers==5.8.0

# === Web / API server ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# === Data handling / preprocessing ===
pandas>=2.2.2
numpy>=1.26.4
python-dotenv>=1.0.1
scikit-learn==1.8.0
rank-bm25>=0.2.2
# 한국어 형태소 토크나이저. 희소 검색(BM25/FTS5) 색인·질의 양쪽에 쓰인다.
# 골든 154쌍 실측(scripts/evaluate_retrieval.py)에서 경량 폴백 대비
# MRR 0.5673 → 0.5934, nDCG@20 0.5217 → 0.5355, 문항 개선 32·악화 20.
# 지연은 동일했다(p50 93ms). 개선폭이 크지는 않지만 비용이 없어 채택했다.
# ⚠️ 설치 여부가 바뀌면 어휘가 통째로 달라진다. 빼거나 더할 때는 반드시
#    scripts/rebuild_lexical_indices.py 로 재색인할 것(안 하면 색인과 질의가
#    갈려 히트만 조용히 사라진다 — fts_index.load_fts_index가 error를 남긴다).
kiwipiepy>=0.21.0
pyarrow>=16.0.0
openpyxl>=3.1.5        # 교과과정 Excel 원본을 pandas.read_excel로 읽기 위한 런타임 의존성
beautifulsoup4>=4.12.0 # Added for web scraping
lxml>=4.9.0            # Added for web scraping (BeautifulSoup's parser)
pdfplumber>=0.11.0     # D-Flex 학식 PDF 식단표 텍스트/표 추출
pypdf>=5.0.0           # 학번별 학사제도/학업이수 가이드 PDF 파싱
APScheduler>=3.10.0    # 공지/학식 데이터 주기적 자동 갱신(in-process 스케줄러)

# === Utility / reranking / fuzzy matching ===
rapidfuzz>=3.9.0
tqdm>=4.66.4
# Optional: install kiwipiepy>=0.21.0 to use Kiwi for Korean TF-IDF tokenization.

# === Optional (for local dev) ===
requests>=2.31.0
jsonschema>=4.22.0
pydantic>=2.7.4
sqlalchemy>=2.0.0
pytest>=8.4.0
pytest-asyncio>=1.0.0
